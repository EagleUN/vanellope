<%= form_with(model: funcionara, local: true) do |form| %>
  <% if funcionara.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(funcionara.errors.count, "error") %> prohibited this funcionara from being saved:</h2>

      <ul>
      <% funcionara.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :info %>
    <%= form.text_field :info %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
